import javax.swing.*;

public class View extends JFrame {
    public View() {
        JButton button = new JButton();

        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setLayout(null);
        setSize(400, 600);
        setVisible(true);

    }
}
